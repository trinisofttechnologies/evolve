 Meteor.methods({
        "testBhavesh" : function(){
            console.log("testBhavesh");
            // Timed.remove({});
            // TimeData.remove({});
            // ranker.remove({});
            // Esquire.remove({});
            // Lesson.remove({});
            // LessonData.remove({});
            // browse.remove({});
            // browseData.remove({});
            // Themes.remove({});
            // ThemesData.remove({});
            // TedTopic.remove({});
            // Tedwatch.remove({});
            // TedwatchData.remove({});
             //TedxTopic.remove({});
            // Tedxwatch.remove({});
            // TedxwatchData.remove({});
            // Loader.remove({});
            // ConversationEvolve.remove({});
            // UserEvolve.remove({});
            // VideoEvolve.remove({});
        }
})
